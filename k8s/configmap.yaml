apiVersion: v1
kind: ConfigMap
metadata:
  name: checkout-api-config
  namespace: checkout-system
data:
  LOG_LEVEL: "info"
  PORT: "5000"
  # DATABASE_URL and REDIS_URL must come from the checkout-api-dependencies Secret.
